# Vim

`:w` = write
`:x` = write and quit
`:q` = quit
`:q!` = saviour of poor souls
`%` = select whole file
`!` = filter

## Useful commands

`:%!xxd` = hex view and edit
`:%!xxd -r` = parse hex back to a file
