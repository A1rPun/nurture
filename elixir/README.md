# Elixir

```
$ iex {module}.ex

Interactive elixir
iex(1)> {module}.{function}
:ok
```

**One-liner**
```
$ elixir -r {module}.ex -e '{module}.{function}'
```

Or create a binary
```
$ elixirc {program}.ex
$ elixir -e '{module}.{function}'
```
